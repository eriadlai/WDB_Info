@model CRUD.Data.ViewModels

@{
    ViewData["Title"] = "Lista de Usuarios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>USUARIOS INDEX</h1>
<div class="container">
    @if (TempData["message"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show " role="alert">
            @TempData["message"]
            <button type="button" class="close" date-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }
    <div class="row">
        <div class="col-sm-6">
            <h3>Lista de Usuarios</h3>
        </div>
        <div class="col-sm-6">
            <a class="btn btn-success d-block" asp-action="Create" asp-controller="Usuarios">Agregar Usuario</a>
        </div>
    </div>
    <div class="row">

        @if (Model.listaUsuarios.Count() > 0)
        {
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <td>@Html.DisplayNameFor(m => m.listaUsuarios.First().id)</td>
                        <td>@Html.DisplayNameFor(m => m.listaUsuarios.First().email)</td>
                        <td>@Html.DisplayNameFor(m => m.listaUsuarios.First().nacimiento)</td>
                        <td>@Html.DisplayNameFor(m => m.listaUsuarios.First().nombre)</td>
                        <td>@Html.DisplayNameFor(m => m.listaUsuarios.First().apellido)</td>
                        <td>@Html.DisplayNameFor(m => m.listaUsuarios.First().profesion)</td>
                        <td>Acciones</td>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model.listaUsuarios)
                    {
                        <tr>
                            <td>@item.id</td>
                            <td>@item.email</td>
                            <td>@item.nacimiento</td>
                            <td>@item.nombre</td>
                            <td>@item.apellido</td>
                            <td>@item.profesion</td>
                            <td>
                                <a asp-controller="Usuarios" asp-action="Edit" asp-route-id="@item.id" class="btn btn-warning">Editar</a>
                                <a asp-controller="Usuarios" asp-action="Delete" asp-route-id="@item.id" class="btn btn-danger">Delete</a>
                            </td>

                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        { <p>No existen Registros.</p>}

    </div>
</div>

